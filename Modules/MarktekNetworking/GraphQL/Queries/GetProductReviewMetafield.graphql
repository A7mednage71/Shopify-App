query GetProductReviewMetafield($productId: ID!, $language: LanguageCode) @inContext(language: $language) {
  product(id: $productId) {
    id
    metafield(namespace: "reviews", key: "items") {
      key
      namespace
      value
      type
    }
  }
}
